<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - Admin</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>

<body class="bg-slate-100">

    <div class="flex h-screen">

        <!-- SIDEBAR -->
        <aside class="w-64 bg-slate-900 text-slate-100 flex flex-col">
            <div class="px-6 py-4 text-xl font-bold border-b border-slate-700">
                Admin Panel
            </div>

            <nav class="flex-1 px-4 py-6 space-y-2">
                <a asp-area="Admin"
                   asp-controller="FormBuilder"
                   asp-action="Index"
                   class="block px-4 py-2 rounded hover:bg-slate-800">
                    📝 Forms
                </a>

                <a asp-area="Admin"
                   asp-controller="Reports"
                   asp-action="Index"
                   class="block px-4 py-2 rounded hover:bg-slate-800">
                    📊 Reports
                </a>

                <a asp-controller="Dashboard"
                   asp-action="UserList"
                   class="block px-4 py-2 rounded hover:bg-slate-800">
                    👥 Users
                </a>

                <!-- Manage Organization Dropdown -->
                @* <div class="relative">
                    <button onclick="toggleOrgDropdown()"
                            class="w-full text-left block px-4 py-2 rounded hover:bg-slate-800 flex items-center justify-between">
                        <span class="flex items-center">
                            <span class="mr-2">👥</span>
                            Manage Organization
                        </span>
                        <i class="fas fa-chevron-down text-xs"></i>
                    </button>

                    <div id="orgDropdown"
                         class="absolute left-0 mt-1 bg-slate-800 border border-slate-700 rounded-lg shadow-lg w-64 py-2 hidden z-50">

                        <a href="#" class="flex items-center px-4 py-2 text-sm hover:bg-slate-700">
                            <div class="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center text-white text-xs mr-3">
                                A
                            </div>
                            <div>
                                <div class="font-medium">ABC Corporation</div>
                                <div class="text-xs text-slate-400">5 members</div>
                            </div>
                        </a>

                        <a href="#" class="flex items-center px-4 py-2 text-sm hover:bg-slate-700">
                            <div class="w-8 h-8 rounded-full bg-purple-500 flex items-center justify-center text-white text-xs mr-3">
                                T
                            </div>
                            <div>
                                <div class="font-medium">Tech Solutions</div>
                                <div class="text-xs text-slate-400">12 members</div>
                            </div>
                        </a>

                        <a href="#" class="flex items-center px-4 py-2 text-sm hover:bg-slate-700">
                            <div class="w-8 h-8 rounded-full bg-green-500 flex items-center justify-center text-white text-xs mr-3">
                                D
                            </div>
                            <div>
                                <div class="font-medium">Design Studio</div>
                                <div class="text-xs text-slate-400">8 members</div>
                            </div>
                        </a>

                        <div class="border-t border-slate-700 mt-2 pt-2">
                            <a href="#" class="flex items-center px-4 py-2 text-sm hover:bg-slate-700">
                                <i class="fas fa-plus-circle mr-3 text-green-400"></i>
                                Add New Organization
                            </a>
                        </div>
                    </div>
                </div> *@

                <a href="#"
                   class="block px-4 py-2 rounded hover:bg-slate-800">
                    ⚙️ Settings
                </a>
            </nav>

            <div class="px-4 py-4 border-t border-slate-700">
                <a asp-controller="User"
                   asp-action="Login"
                   class="block px-4 py-2 bg-red-600 rounded text-center">
                    Logout
                </a>
            </div>
        </aside>

        <!-- MAIN CONTENT -->
        <div class="flex-1 flex flex-col">

            <!-- TOP BAR -->
            <header class="h-14 bg-white border-b flex items-center px-6 shadow-sm">
                <h1 class="text-lg font-semibold">
                    @ViewData["Title"]
                </h1>
            </header>

            <!-- PAGE CONTENT -->
            <main class="flex-1 p-6 overflow-auto">
                @RenderBody()
            </main>
        </div>

    </div>

    <script>
        function toggleOrgDropdown() {
            const dropdown = document.getElementById('orgDropdown');
            dropdown.classList.toggle('hidden');

            // Close dropdown when clicking outside
            document.addEventListener('click', function(event) {
                if (!event.target.closest('.relative')) {
                    dropdown.classList.add('hidden');
                }
            });
        }
    </script>

</body>
</html>